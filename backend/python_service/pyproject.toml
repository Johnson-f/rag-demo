[project]
name = "python-service"
version = "0.1.0"
description = "CSV processing service for trade data import"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pandas>=2.0.0",
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
]
